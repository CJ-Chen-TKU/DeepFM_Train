# App/UI
streamlit>=1.29

# Core ML stack
torch>=2.1
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24

# Excel readers
openpyxl>=3.1     # for .xlsx/.xlsm
xlrd==1.2.0       # for legacy .xls (optional but included)

# Multi-label stratified split (optional but recommended)
iterstrat>=0.3
